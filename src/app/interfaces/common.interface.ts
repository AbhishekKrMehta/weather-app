export interface Temperature {
  value: number;
  unit: string;
}

export interface WindSpeed {
  value: number;
  unit: string;
}

export interface Coordinates {
  latitude: number;
  longitude: number;
}
